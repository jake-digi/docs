---
title: "Create Room"
api: "POST /api/v1/rooms"
method: "POST"
---

## Create Room

Create a new room within a block. Room numbers must be unique within the same block.

<ParamField body="room_number" type="string" required>
  Room number
</ParamField>

<ParamField body="block_id" type="string" required>
  Block ID the room belongs to
</ParamField>

<ParamField body="instance_id" type="string" required>
  Instance ID
</ParamField>

<ParamField body="name" type="string">
  Room name
</ParamField>

<ParamField body="capacity" type="number">
  Room capacity
</ParamField>

<Warning>
  - Room numbers must be unique within the same block
  - Block must exist and belong to the same instance
</Warning>

### Example Request

```bash
curl -X POST https://api.checkpoint.b-digital.uk/api/v1/rooms \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "room_number": "101",
    "block_id": "block123",
    "instance_id": "se8SajpJDWUwrEmhElLF",
    "name": "Room 101",
    "capacity": 4
  }'
```
